{
  "name": "iiko-dashboard-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --check server.js && node --check iikoClient.js && node --check dashboardService.js && node --check sessionStore.js && node --check userStore.js && node --check auditLog.js && node --check notifier.js && node --check tokenStore.js && node --check totp.js && node --check scheduler.js && echo OK"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "express-rate-limit": "^7.5.1",
    "helmet": "^7.2.0",
    "otplib": "^13.4.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
